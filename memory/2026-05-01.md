# 2026-05-01

- Heartbeat stabilization pass: found `scripts/validate-suite.sh` was failing early because `behavioral-enforcement.py quick` returns exit 1 in watchful mode; patched suite to continue unless enforcement reaches strict/blocking level (>=3).
- Verified `openclaw status` and `openclaw status --deep`: gateway running, Slack configured, health reachable.
- Remaining notable risk from deep security audit: small-model fallback `ollama/llama3.1:8b` was configured without sandboxing and with web tools enabled; narrowed provider-level web denies were insufficient because the audit keys off global tool exposure.
- Stabilization follow-up: removed `ollama/llama3.1:8b` from `agents.defaults.model.fallbacks` and verified `openclaw security audit` now reports 0 critical findings against the config file. File state is correct, but the gateway still needs a restart before live runtime/config-get reflects the new fallback chain.
- Package A executed/verified: live config now uses `openai-codex/gpt-5.4` as primary with `anthropic/claude-opus-4-6` as the sole general fallback; provider-level defense-in-depth deny remains for `ollama/llama3.1:8b` (`group:web`, `browser`). `openclaw status --deep` healthy and `openclaw security audit` shows 0 critical findings.
- SH-1 progress: added `superhumanMail` MCP server config to OpenClaw (`npx -y @superhuman/mcp-mail`), completed Superhuman OAuth, and verified the MCP proxy can now connect successfully to `https://mcp.mail.superhuman.com/mcp` using cached auth. Next step is using the read-only pilot workflows in practice and confirming whether Superhuman read-only mode was applied on the account side.
- SH-1 pilot checklist run: successful inbox/calendar briefing, successful thread retrieval/summarization on Gold Coast Auto Body thread `19ac24acf0816942`, and successful draft-only reply creation (`draft004fe861c07a3640`) without sending. The main remaining caveat is that I still cannot independently prove whether Superhuman enforced a read-only permission boundary during auth; operationally, we should continue treating the integration as read-mostly until we deliberately test write controls.
- Legacy Google watcher cleanup: removed the old `hooks.gmail` watcher/push block from `~/.openclaw/openclaw.json` after confirming Superhuman is now the intended mail/calendar surface. Verified `superhumanMail` MCP remains configured and `openclaw security audit` stayed at `0 critical · 1 warn · 2 info` after cleanup.
- GPT-1 setup: added `openai-codex/gpt-5.5` to the configured model catalog without changing the main default model. Initial isolated probe silently landed on GPT-5.4 before the catalog entry existed; after adding the entry, a second isolated probe confirmed real GPT-5.5 availability. Because the current webchat surface cannot host thread-bound persistent subagent sessions, the practical GPT-1 pilot pattern is on-demand one-shot GPT-5.5 subagent runs for selected high-judgment tasks while the main session stays on GPT-5.4.
- Slack fix pass via GPT-1: confirmed the root cause was `channels.slack.groupPolicy="allowlist"` with only one allowlisted Slack channel in config despite a multi-channel routing model. Resolved and allowlisted the topic channels for `pb-parking-lot`, `pb-important-email`, `pb-email-low-priority`, `pb-job-search`, `pb-daily-brief`, `pb-ops-status`, `pb-reading-queue`, `pb-gaming-channel`, and the Timberland lane after confirming the real channel name is `pb-timberland-band-app` (`C0AMV56Q84W`). Extra channels like `pb-granola-app`, `pb-lucid-lease`, `pb-revenue-project`, and `pb-stock-market` exist but are not part of the current routing-default set.
- Slack operating-model redesign recommendation: move to a lean 4-surface core (`main DM`, `#pb-parking-lot`, `#pb-ops-status`, `#pb-daily-brief`) plus active-work channels only when a lane is actually live (`#pb-job-search`, `#pb-gaming-channel`, `#pb-lucid-lease`, `#pb-revenue-project`, `#pb-timberland-band-app`). Recommended retiring `#pb-important-email`, `#pb-email-low-priority`, and `#pb-reading-queue` from default routing in favor of stronger curation and digest behavior.
- Slack operating-model activation: live Slack allowlist was narrowed to the lean core + active-work channels only, and `requireMention` was set to `false` on those managed channels so Chris can talk to Shelia naturally in them without explicit mentions. This keeps access bounded while making Shelia operationally responsible for the environment instead of passive unless pinged.
- Slack runtime follow-up: the gateway restart did apply and the live Slack allowlist now shows the lean 8-channel managed set, but the restart also surfaced a deeper blocker: the Slack plugin failed during register with `Error: Cannot find module '../dist/babel.cjs'` from `.../openclaw/dist/extensions/slack/index.js`. That likely explains why Slack behavior is still unreliable despite status output showing Slack as configured.
- Deeper Slack diagnosis: the startup failure was not a Slack logic bug but a staged plugin runtime-deps cache problem. The active gateway runs the NVM OpenClaw install at `/Users/productivebot/.nvm/versions/node/v24.13.1/lib/node_modules/openclaw`, while `which openclaw` also points at a separate Homebrew install under `/opt/homebrew/lib/node_modules/openclaw`, making diagnostics easy to aim at the wrong root. The failing staged cache was `/Users/productivebot/.openclaw/plugin-runtime-deps/openclaw-2026.4.29-3a5f0ffb0187`; at failure time `jiti/lib/jiti.mjs` existed but `jiti/dist/babel.cjs` was missing. The correct repair path is to operate against the active NVM package root with `openclaw plugins deps --package-root /Users/productivebot/.nvm/versions/node/v24.13.1/lib/node_modules/openclaw --repair`, then restart.
- Slack recovery progress: after a full forced rebuild/restage of the plugin runtime-deps cache and another gateway restart, Slack status returned to `ON / OK`, the managed Slack groups were present again, and the latest post-restart log slice no longer showed a fresh repeat of the Slack `../dist/babel.cjs` register failure. At last check, infrastructure looked healthy, but end-to-end reply behavior in the main Slack DM plus `#pb-parking-lot`, `#pb-ops-status`, and `#pb-daily-brief` still required real message testing to fully confirm.
- Slack test diagnosis: the 22:35 CDT Slack DM `new test` did ingest and reply, but with poor latency; the same test phrase did not appear in any matching managed-channel session transcript, suggesting the remaining failure is channel intake before agent/session creation rather than the already-fixed plugin startup bug.
- Slack next-step recommendation: favor a clean rebuild of the OpenClaw↔Slack integration boundary instead of more incremental patching. Wrote a concrete phased plan at `/Users/productivebot/.openclaw/workspace/slack-clean-rebuild-plan-2026-05-01.md` covering archive, clean disable, fresh app baseline, DM-only proof, single-channel proof, and controlled rollout.
